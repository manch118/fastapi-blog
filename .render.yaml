services:
  - type: web
    name: fastapi-app
    env: python
    runtime: python
    pythonVersion: 3.11.11      # явно задаем версию
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      uvicorn main:app --host 0.0.0.0 --port 10000